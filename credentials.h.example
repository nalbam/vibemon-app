/**
 * WiFi and WebSocket Credentials Template
 *
 * Copy this file to credentials.h and update with your credentials.
 * credentials.h is ignored by git to prevent accidental exposure.
 *
 * WiFi Provisioning (NEW):
 * ========================
 * If USE_WIFI is enabled but no WiFi credentials are saved, the device will
 * automatically start in provisioning mode:
 * 
 * 1. ESP32 creates an Access Point with:
 *    SSID: "VibeMon-Setup"
 *    Password: "vibemon123"
 * 
 * 2. Connect to this network with your phone/laptop
 * 
 * 3. A captive portal will open automatically (or navigate to 192.168.4.1)
 * 
 * 4. Select your WiFi network and enter the password
 * 
 * 5. (Optional) Enter VibeMon WebSocket token for authentication
 * 
 * 6. Device will save credentials and reboot to connect to your WiFi
 * 
 * WiFi credentials and WebSocket token are stored in device flash memory 
 * and persist across reboots.
 * 
 * To reset WiFi settings, send POST request to: http://DEVICE_IP/wifi-reset
 *
 * Usage:
 * 1. cp credentials.h.example credentials.h
 * 2. Flash the firmware (WiFi and WebSocket are enabled by default)
 * 3. Device will enter provisioning mode on first boot
 * 4. Configure WiFi, WebSocket token via captive portal
 * 
 * Advanced:
 * - To disable WiFi: Comment out #define USE_WIFI
 * - To disable WebSocket: Comment out #define USE_WEBSOCKET
 * - To set default credentials: Edit WIFI_SSID, WIFI_PASSWORD, WS_TOKEN
 * 
 * Note: WIFI_SSID, WIFI_PASSWORD, and WS_TOKEN below are optional. 
 * If not defined, the device will start in provisioning mode on first boot
 * where you can configure them via the web interface.
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// =============================================================================
// WiFi Configuration
// =============================================================================
// WiFi mode enabled by default (required for WebSocket)
// Comment out to disable WiFi features
#define USE_WIFI

// Optional: Default WiFi credentials (used only if no saved credentials exist)
// If not defined, device will start in provisioning mode automatically
#define WIFI_SSID "YOUR_SSID"
#define WIFI_PASSWORD "YOUR_PASSWORD"

// =============================================================================
// WebSocket Configuration (optional)
// =============================================================================
// WebSocket client enabled by default (requires USE_WIFI)
// Comment out to disable WebSocket features
#define USE_WEBSOCKET

// WebSocket server URL (without protocol prefix)
// Examples:
//   "ws.vibemon.io"   (port 443)
//   "127.0.0.1"       (custom port)
#define WS_HOST "ws.vibemon.io"
#define WS_PORT 443
#define WS_PATH "/"

// Use secure WebSocket (wss://) - set to true for HTTPS servers
#define WS_USE_SSL true

// Access token for authentication (optional)
// Note: This can now be configured via the WiFi provisioning interface.
// If not defined here, you can set it during device setup or leave empty.
#define WS_TOKEN "your_access_token"

#endif // CREDENTIALS_H
