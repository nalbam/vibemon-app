/**
 * WiFi Credentials Template
 *
 * Copy this file to credentials.h and update with your WiFi credentials.
 * credentials.h is ignored by git to prevent accidental exposure.
 *
 * Usage:
 * 1. cp credentials.h.example credentials.h
 * 2. Edit credentials.h with your actual WiFi SSID and password
 * 3. Uncomment #define USE_WIFI in vibe-monitor.ino
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

#define WIFI_SSID "YOUR_SSID"
#define WIFI_PASSWORD "YOUR_PASSWORD"

#endif // CREDENTIALS_H
