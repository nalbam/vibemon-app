# Vibe Monitor Environment Variables
# Supports: Claude Code, Kiro IDE, Kiro CLI
#
# Setup (choose one):
#
# Option 1: Copy to IDE config folder (auto-loaded by hook script)
#   - Claude Code: cp hooks/.env.sample ~/.claude/.env.local
#   - Kiro:        cp hooks/.env.sample ~/.kiro/.env.local
#
# Option 2: Source from shell profile (~/.zshrc or ~/.bashrc)
#   if [ -f ~/.claude/.env.local ]; then
#     source ~/.claude/.env.local
#   fi

# Debug mode (1: enabled, 0: disabled)
# export DEBUG=1

# Desktop App path (empty to disable)
# Auto-launches on session start if not running (e.g., ~/vibe-monitor/desktop)
export VIBE_MONITOR_DESKTOP=""

# Desktop App URL (empty to disable)
# HTTP endpoint for Desktop App (e.g., http://127.0.0.1:19280)
export VIBE_MONITOR_URL=""

# ESP32 status display (only sends if configured, empty to disable)
# USB Serial port (e.g., /dev/cu.usbserial-0001, /dev/ttyUSB0)
# Check with: ls /dev/cu.* or ls /dev/tty*
export ESP32_SERIAL_PORT=""
# HTTP (e.g., http://192.168.1.100)
export ESP32_HTTP_URL=""
