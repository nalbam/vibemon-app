# Claude Code Environment Variables
# Copy this file to ~/.claude/.env.local and set the values.

# Debug mode (1: enabled, 0: disabled)
# export DEBUG=1

# Vibe Monitor Desktop App path (empty to disable)
# Auto-launches on SessionStart if not running (e.g., ~/vibe-monitor/desktop)
export CLAUDE_MONITOR_DESKTOP=""

# Vibe Monitor URL (empty to disable)
# HTTP endpoint for Desktop App (e.g., http://127.0.0.1:19280)
export CLAUDE_MONITOR_URL=""

# Vibe Monitor Character
# Available characters: clawd (default), kiro, or any custom character defined in CHARACTER_CONFIG
# To add new characters, update CHARACTER_CONFIG in the source files
export CLAUDE_MONITOR_CHARACTER="clawd"

# ESP32 status display (only sends if configured, empty to disable)
# USB Serial port (e.g., /dev/cu.usbserial-0001, /dev/ttyUSB0)
# Check with: ls /dev/cu.* or ls /dev/tty*
export ESP32_SERIAL_PORT=""
# HTTP (e.g., http://192.168.1.100)
export ESP32_HTTP_URL=""
